import styled from "styled-components";

export const Top = () => {
  return (
    <SContsinar>
      <h2>ユーザー一覧</h2>
    </SContsinar>
  );
};

const SContsinar = styled.div`
  text-align: center;
`;
